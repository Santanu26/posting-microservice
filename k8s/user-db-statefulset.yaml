apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: user-db
spec:
  selector:
    matchLabels:
      app: user-db
  serviceName: user-db-service
  replicas: 1
  template:
    metadata:
      labels:
        app: user-db
    spec:
      containers:
        - name: user-db
          image: mysql:8.0.33
          env:
            - name: MYSQL_DATABASE
              value: user_db
            - name: MYSQL_USER
              value: root
            - name: MYSQL_PASSWORD
              value: qwerty
          ports:
            - containerPort: 3306
          volumeMounts:
            - name: user-db-pvc
              mountPath: /var/lib/mysql
  volumeClaimTemplates:
    - metadata:
        name: user-db-pvc
      spec:
        accessModes:
          - ReadWriteOnce
        resources:
          requests:
            storage: 1Gi
